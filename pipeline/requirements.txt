# Pinned dependencies for the Earth Engine quarterly pipeline.
# All versions == so a quarterly rerun a year from now uses the same EE API
# semantics and produces the same per-city z-scored composite.
#
# Unused-by-pipeline dependencies (geopandas, pandas, pyarrow, shapely)
# were removed; they were never imported. The detection pipeline's
# requirements.txt at repo root covers the ML stack.

earthengine-api==1.7.9
requests==2.32.5
beautifulsoup4==4.12.3
pillow==12.2.0
